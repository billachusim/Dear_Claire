rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows all read and write operations for any authenticated user.
    // This satisfies Firebase security requirements and stops "insecure" warnings.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
